# Bac à sable

Faux dépôt servant de terrain de jeu à l'Agent v0.1.

Le module `facturation.py` calcule des totaux de facture. La suite de tests échoue : c'est
à l'agent de trouver pourquoi.

```sh
python -m pytest -q
```

> Ne corrige pas le bug toi-même : il est volontaire, et c'est l'objet de l'exercice.
